{
  "name": "lotte-roaming-server",
  "private": true,
  "version": "0.1.0",
  "description": "BFF for LOTTE ROAMING — auth, payments, eSIM provisioning, push. Runs on mocks; real Kakao OAuth + PortOne payment gated by env.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.19.21",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
